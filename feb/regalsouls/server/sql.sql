CREATE TABLE readings(
     id SERIAL PRIMARY KEY,
     first VARCHAR(255),
     last VARCHAR(255),
     email VARCHAR(255),
     phone VARCHAR(255),
     birthdate VARCHAR(255),
     target VARCHAR(255)
)